import Link from "next/link";
import type { PublicBusinessSettings } from "@/lib/settings/service";

export function Footer({ settings }: { settings: PublicBusinessSettings }) {
  return <footer className="mt-auto border-t border-[var(--line)] bg-[var(--brand-dark)] text-slate-200"><div className="site-container grid gap-8 py-10 md:grid-cols-[1.2fr_1fr_1fr] md:items-start"><div><p className="text-lg font-extrabold text-white">{settings.name}</p><p className="mt-2 max-w-xs text-sm leading-6 text-slate-300">{settings.description}</p></div><div className="grid gap-2 text-sm"><p className="font-bold text-white">Visit or call</p><a href={settings.phoneHref} className="hover:text-white">{settings.phone}</a><a href={`mailto:${settings.email}`} className="break-all hover:text-white">{settings.email}</a><p>{settings.address}</p>{settings.mapsUrl && <a href={settings.mapsHref} target="_blank" rel="noreferrer" className="hover:text-white">Open in Google Maps</a>}</div><div className="grid gap-2 text-sm"><p className="font-bold text-white">Business hours</p><p>All Days (9:00 AM to 8:00 PM)</p><Link href="/contact" className="mt-2 hover:text-white">Contact us</Link><Link href="/admin/login" className="hover:text-white">Admin login</Link></div></div><div className="site-container border-t border-white/15 py-4 text-xs text-slate-400">Ⓒ  Copyright {new Date().getFullYear()} {settings.name}. All rights reserved.</div></footer>;
}
